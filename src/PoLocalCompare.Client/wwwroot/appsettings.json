{
  "Auth": {
    // Guest/dev bypass. The client also requires a non-Production environment
    // (see NavMenu.razor / Login.razor), so this is effectively dev+test only,
    // mirroring the server's !IsProduction() gate on /auth/login/fake.
    "AllowGuest": true
  },
  "BrowserModels": {
    "PrimaryCdnBaseUrlTemplate": "https://huggingface.co/mlc-ai/{modelId}/resolve/main/",
    "CdnBaseUrlTemplate": "https://huggingface.co/mlc-ai/{modelId}/resolve/main/"
  },
  "Features": {
    "UseRealAi": true
  }
}
