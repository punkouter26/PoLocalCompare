{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "KeyVault": {
    "Uri": "https://kv-poshared.vault.azure.net/"
  },
  "AiJudge": {
    "_VisionEnabled_note": "Shows the judge a screenshot of each rendered page as well as its source. Needs headless Chromium — already installed here for the E2E-UI suite. Off in appsettings.json because the Free-tier App Service has no browser. The judge deployment must accept image input.",
    "VisionEnabled": true
  },
  "Features": {
    "AllowAnonymousWrites": true
  }
}
