## .NET artifacts
bin/
obj/
*.user
*.suo
.vs/
*.lscache

## VS Code — ignore user preferences but keep project launch/task config
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json

## Secrets and environment-specific config
appsettings.Development.json
appsettings.*.json
!appsettings.json
# The API's Development overrides ARE committed and are load-bearing: they hold no secrets
# (empty App Insights connection string, the shared Key Vault URI, Features:AllowAnonymousWrites)
# and both test host fixtures run as Development, so they inherit this file. The blanket rules
# above matched it while git kept honouring it only because it was already tracked — this
# negation makes the intent explicit so a delete-and-re-add cannot silently drop it.
!src/PoLocalCompare.Api/appsettings.Development.json

## OS artifacts
.DS_Store
Thumbs.db
*.tmp
*.swp

## Logs
*.log
logs/

## AI workflow state files
.potune-state.json
run_final.json
run_status.json

## Build outputs
dist/
publish/
artifacts/
appservice-logs/
appservice-logs.zip

## Local LLM model weights (downloaded by download-models.py, ~4.7 GB)
src/PoLocalCompare.Client/wwwroot/models/

## NuGet
*.nupkg
*.snupkg
packages/

## Test results
TestResults/
coverage/
*.trx
*.coveragexml

## Playwright
test-results/
playwright-report/
node_modules/

## Visual Studio additional
.vs/
.vscode/*.log
*.rsuser
.settings/
PublishProfiles/

## IDE specific
.idea/
*.iml
*.swp
*.swo
*~

## Application logs (local development)
src/PoLocalCompare.Api/logs/
src/PoLocalCompare.Client/logs/

## Docker & Container
docker-compose.override.yml
*.docker-compose.override.yml

## Azure
.azure/
local.settings.json
.env.local
.env.*.local
.env

## Certificates & secrets
*.pfx
*.p12
*.cer
*.key
*.pem

## Zip artifacts
*.zip

## Package managers
package-lock.json
yarn.lock
pnpm-lock.yaml

## Performance profiling
*.etl
*.nettrace
*.diagsession

## Temporary files
*.bak
*.temp
*.tmp
temp/
.temp/

## Rider IDE
*.sln.iml
*.DotSettings.user

## Benchmark outputs
BenchmarkDotNet.Artifacts/

## OS specific
*.exe
*.com
*.bat
*.cmd

## AI/ML models (if large)
**/*.onnx
**/*.pb
**/*.pth
**/*.pt

## Azure App Service
scm_restartfile

## Generated model-test reports (SCRIPTS/build_html_report.py, test-browser-models.cjs)
model-test-report.html
model-test-status.tsv
browser-test-status.tsv

## DOCS/ is a committed deliverable, not a build output.
## DOCS/diagrams/*.mmd are sources; DOCS/assets/*.svg are compiled from them with
##   npx @mermaid-js/mermaid-cli -i DOCS/diagrams/<name>.mmd -o DOCS/assets/<name>.svg -b transparent
## Both are tracked on purpose so the reports render without a build step. Nothing here is ignored.
