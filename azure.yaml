# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: polocalcompare
metadata:
  template: polocalcompare@1.0.0

infra:
  provider: bicep
  path: infra
  module: main

services:
  api:
    project: src/PoLocalCompare.Api
    language: dotnet
    host: appservice
